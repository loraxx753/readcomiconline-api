import { unauthorized, invalidLogin } from './unauthorized';
import { unknown } from './unknown';
import { emptySuccess } from './empty_success';

export default {
  // SUCCESS
  emptySuccess,
  // ERROR
  unauthorized,
  invalidLogin,
  unknown
};
